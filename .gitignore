*.root
*.npz
graal_data/
.DS_Store
**.DS_Store**


__pycache__/
*.pyc

.venv/
*.egg-info/
.vscode/
*cache*

docs/
.superpowers/
.vscode/

# Test data: nothing under here is tracked. --test-data redirects the detector
# directories onto this folder; create it and fill it yourself (see Testing in
# the wiki). The skeleton and README that used to be tracked were a convenience,
# not a contract.
test_data/

# Anchored to the repo root: an unanchored `data/` matches at any depth and
# would also swallow 04_mc_simulation/data/ and 05_analysis_bdt/data/, which
# hold generated Monte Carlo and feature matrices — untracked for their size,
# by the *.root and *.npz rules above, not by this one.
/data/

# Plot output is a result, not source: it is derived from data/analyzed/ by
# `python -m plots.dalitz` and regenerated whenever the reconstruction changes.
# Tracking it put binary churn in every diff that touched the analysis, and let
# the repo carry figures that could silently disagree with the code that drew
# them. The folder itself stays (.gitkeep) so the stage has somewhere to write.
06_plots/plots/*
!06_plots/plots/.gitkeep
